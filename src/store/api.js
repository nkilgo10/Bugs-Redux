import { createAction } from "@reduxjs/toolkit";

export const callBegan = createAction("callBegan");
export const callSuccess = createAction("callSuccess");
export const callFailed = createAction("callFailed");


